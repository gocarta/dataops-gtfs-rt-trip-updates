# Contributing

## Getting Setup on Amazon Linux
```
# install git
sudo dnf install git -y

# install uv, which we use to run Python
curl -LsSf https://astral.sh/uv/install.sh | sh

# install cronie for the crontab
sudo dnf install cronie -y
```